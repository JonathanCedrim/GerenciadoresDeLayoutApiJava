<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="br.com.amora.projeto.gerenciadoresdelayoutapijava">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" android:label="MAIN"/>
        <activity android:name=".activity.ListActivity"/>
        <activity android:name=".activity.TableActivity"/>
        <activity android:name=".activity.LinearLayoutAPIJavaActivity" android:label="API JAVA"/>
        <activity android:name=".activity.InflateActivity" android:label="API INFLATER"/>
        <activity android:name=".activity.AutoCompletActivity" android:label="Auto Complet"/>
        <activity android:name=".activity.ImgButtonActivity" android:label="Img Button"/>
        <activity android:name=".activity.SpinnerActivity"/>
        <activity android:name=".activity.DialogActivity"/>
        <activity android:name=".activity.ProgressBarActivity"/>
        <activity android:name=".activity.ToastActivity"/>
        <activity android:name=".activity.AlertDialogActivity"/>
    </application>
</manifest>
